#ifndef MAINSCREEN_H
#define MAINSCREEN_H

/*#include "screen.h"
#include "string.h"

class MainScreen : public Screen{
	public:
		MainScreen(GameData *gameData, Window *window);
		void draw();
		void handleInput();
	private:

};*/
#endif
